const subnavStyles=document.createElement("link");subnavStyles.rel="stylesheet";subnavStyles.href="subnav.css";document.head.appendChild(subnavStyles);const params=new URLSearchParams(location.search);const play=params.get("play");const topic=document.querySelector("#topic");if(topic&&play){const option=document.createElement("option");option.value=`Play ${play}`;option.textContent=`Play ${play}`;option.selected=true;topic.prepend(option)}document.querySelectorAll("[data-demo-form]").forEach(form=>form.addEventListener("submit",event=>{event.preventDefault();const status=form.querySelector(".status");status.hidden=false;status.textContent="Thank you. This preview form is ready to connect to the selected form or email service before launch.";form.querySelector("button").disabled=true}));
