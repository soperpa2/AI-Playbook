const pathwayPhases=["plan","build","deploy","govern"];document.querySelectorAll(".path li").forEach((item,index)=>{const phase=pathwayPhases[index];const link=document.createElement("a");link.href="#plays";link.className="pathway-link";while(item.firstChild)link.appendChild(item.firstChild);item.appendChild(link);link.addEventListener("click",()=>{const button=document.querySelector(`.tabs button[data-phase="${phase}"]`);if(button)button.click()})});
